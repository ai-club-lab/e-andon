# dashboard service (design.md §4.4, §7) — API + SSE + chat + HITL
fastapi==0.139.0
sse-starlette==2.1.3
uvicorn[standard]==0.34.0
jinja2==3.1.5                   # lightweight server-rendered front (§12 decision 1)
asyncpg==0.30.0
sqlalchemy[asyncio]==2.0.36
-e ../../packages/shared
psycopg[binary]==3.2.3
slack_sdk==3.33.5               # notification sink + inbound signature verify (human-loop)
python-multipart==0.0.20        # field-photo upload (/correct/attachment)
