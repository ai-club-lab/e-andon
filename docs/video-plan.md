# 提出動画 構成台本（3分想定）

審査5軸: ①AIエージェントが価値の中心（自律性・必然性）②課題へのアプローチ力（ストーリー）
③ユーザビリティ ④実用性・体験価値（突き抜け加点）⑤実装力（技術選定の納得度・拡張性・実運用配慮）。
テーマ: つくる・まわす・とどける。

## 0:00–0:20 課題フック（軸②）

- 映像: タイトル →「チョコ停」の説明1枚（実映像1カット＋テロップ）
- 台本: 「生産ラインの微小停止『チョコ停』。**止まったことは分かるが、なぜかは人が設備を
  見て回って調べる** — 復旧は数分、原因調査は数十分から翌日持ち越し。計画稼働480分の
  ラインで累計60分なら、それだけで稼働率▲12.5%です」
- テロップ: `復旧は数分 ／ 原因調査は数十分〜翌日` `▲12.5%`

## 0:20–1:20 デモ本編 — 1本の閉ループを実時間で（軸①③④）

**2画面構成: PC=Webダッシュボード、スマホ=Slack（通知が「鳴る」瞬間を映す）**

1. 実映像が流れ、赤枠で整列ズレ検知 → ライン停止（ヘッダが赤に）
   - 台本: 「検知は**OpenCVの決定論**。ラインを止める判断はLLMにさせません」
2. 右のAIエージェントが通知: 消去法の根拠・確信度・**ツール呼び出しの履歴**をズーム
   - 台本: 「ここからがエージェントの仕事。**センサーのない位置決め機構**の異常は、
     ログを1本引けば済む問題ではない。エージェントが**自分でツールを選び**、5つの
     機械センサーを照会して『全て正常』を**消去法の根拠**に変え、過去事例をベクトル
     検索して真因を絞ります。カードに残っているのはその**証跡**です」（軸①の核）
3. スマホ側: Slackカードが**実メンションで佐藤さんを鳴らす** → 👋私が対応します
   → **PCのカードが数秒で「👋 佐藤さんが対応中」に変わる**（面間同期）
   - 台本: 「担当は当番表から自動選定され、本人に**名前で**通知。応答がなければ5分後に
     班長へ — 誰が動くかまでがプロダクトの仕事です」（軸③④）
4. カードの**2枚の写真**をズーム: 検知フレーム（いま）＋過去事例の現場写真（前回の真因の実物）＋前回の処置
   - 台本: 「初動で見るべきものが揃う。**数十分の調査が数十秒に**」

## 1:20–2:00 まわす — 訂正1件が次回の推論を変える（軸①④・最大の見せ場）

1. ✗原因が違った → **AIが対話で聞き取り**「現場では何が原因でしたか？」→
   「ガイドレールのボルトが緩んでいた」→ 事例化の返答
2. 再発（リロード）→ **第一候補が現場の教えた真因に変わっている** Before/After
3. 分析ビュー: 「📚 学習データの蓄積 — **誰が・いつ・何を教えたか**」＋AI正答率の推移
   - 台本: 「訂正はフォームではなく**対話**で回収し、埋め込み化して次回のfew-shotへ。
     AIの賢さの出所が**人の名前で監査できる**。再現スクリプト同梱です」

## 2:00–2:40 実装力・DevOps（軸⑤・テーマ3コンセプト）

- READMEのアーキ図: 「**重い判断は決定論、なぜ？はエージェント** — Cloud Run単一サービス、
  Vertex AI（Gemini 3 Flash）＋ADK、事例はCloud SQL+pgvectorに同居（学習ループが
  1トランザクション空間で閉じる）」
- 当番表ビュー: 「**通知先の決定はこの表の参照のみ。AIには決めさせない**」（ガードレール）
- GitHub Actions: 「push→本番反映**約3分**（キーレスWIF）。実映像241フレームの回帰
  テストが毎push走り、**誤検知0を担保してからしかデプロイされない**」（Four Keys表を一瞬）

## 2:40–3:00 まとめ

- 「検知→停止→原因推定→人の裁定→次回の改善。**1本の閉ループが、いま稼働URLで動いています**」
- 稼働URL・次の一手（✅復旧クローズで停止時間の実測・シフト連動当番）を一言 → 拡張性
- 終画面: URL＋リポジトリ＋「つくる・まわす・とどける」

## 撮影の実務チェックリスト

- [ ] **30分前にウォームアップ1周**（RCAキャッシュ温め・Slackカードの新形式確認）
- [ ] **撮影中〜完了まで git push しない**（デプロイ=瞬断・キャッシュ消失）
- [ ] 学習ループのBefore/Afterは**事前に1回✗訂正を仕込む**と本番テイクが1発で決まる
- [ ] Webの👋チップを一度選び直す（localStorageの旧名を更新）
- [ ] Slack側: 通知チャンネルを最上部・無関係チャンネルのバッジを消す・DND解除
- [ ] ブラウザズーム100%・1080p以上で収録。カードの文字が読める倍率でズーム編集
- [ ] エスカレーションは実時間5分 — 動画では「応答がなければ5分後に班長へ」の**文言をズーム**で
  伝える（待たない）。tier2の実メンションを見せたい場合のみ `ESC_TIER2_DELAY_S=30` で撮影後に戻す
- [ ] テイク間でSlackにカードが溜まったら削除してよい（NOTIF_THROTTLE_S=0で毎再生投稿）
